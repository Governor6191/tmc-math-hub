// AI tutor configuration.
//
// The tutor is OFF until you paste your deployed proxy URL below. This keeps
// your Gemini API key private (it lives on the proxy, never in this site) and
// free for every student. Full setup is in ai/README.md (about 2 minutes):
//   1. Create a free Google Gemini API key (keep billing disabled).
//   2. Deploy the tiny Cloudflare Worker in ai/gemini-worker.js and add the key.
//   3. Paste the Worker URL here and redeploy.
//
// Leave it as an empty string to keep the tutor hidden.
export const AI_ENDPOINT = '';
